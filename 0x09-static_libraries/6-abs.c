#include "main.h"

/**
 * _abs  - checks for lowercase character.
 *@n: ischaracter
 *
 * On error: return 0
 * Return: 1 if success
 */
int _abs(int n)
{

	if (n >= 0)
	{return (n); }
	else
	{return (n * -1); }
}
